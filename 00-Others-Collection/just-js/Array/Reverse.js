//reverse()
// Reverses the oreder of the elemets of an array in place. the first element becomes the last, and the last element
// becomes the first
const numbers = [1, 2, 3]; //original array
numbers.reverse();
console.log(numbers); //[3, 2, 1]
