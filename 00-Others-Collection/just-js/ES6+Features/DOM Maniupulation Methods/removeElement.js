//7.Removing Elements:
//element.removeChild(childElement): Removes a child element from the specifield element.
//element.remove(): Removes the element directly.

div.removeChild(newpara);
//or
newpara.remove();
