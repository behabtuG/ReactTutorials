function recursiveFunction(parameters) {
  if (baseCaseCondition) {
    return baseCaseValue; // Return result when the base case is met
  } else {
    // Recursive call with modified parameters
    return recursiveFunction(modifiedParameters);
  }
}
